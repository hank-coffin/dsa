// solution for snowflakes identification from Algorithmic Thinking
//
// Steps:
//  Assume all snowflakes have same length
//  compare snowflakes left to right
//  compare snowflakes right to left
//  normalize all snowflakes left to right, storing original

fn compare_left_to_right<T>( a : &Vec<T>, b : &Vec<T> ) -> bool {
    let pa : usize = find_min(a);
    let pa : usize = find_min(b);
}

fn main() {
    println!("Hello, world!");
}
